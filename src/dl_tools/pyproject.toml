[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dl-tools"
version = "0.1.0"
description = "Unrestricted local-exec tools for the Deep Investigator build crews"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = ["dl_tools"]